# NEXIVO HUB 테스트 체크리스트

1. 웹 로그인: 오너는 아이디/비밀번호만, 일반 구매자는 라이선스 키까지 입력
2. Discord `/라이센스`: 비활성 서버에서 다른 명령 사용 시 ephemeral 경고
3. `/서버생성`: 오너만 실행 → 템플릿 생성
4. `/서버삭제 confirm:삭제`: 오너만 실행 → 실행 채널만 남는지 확인
5. `/인증패널`: 서버 인증 버튼/웹 인증 링크 생성
6. 웹 인증: Discord OAuth 성공 → 보안 로그 생성 → 봇이 인증 역할 지급
7. 사이트에서 상품 추가/재고 변경 → Discord 자판기/재고 임베드 갱신
8. `/충전` → `/충전대기` → 오너 `/충전승인` → `/잔액` 확인
9. 자판기 `잔액으로 구매` → 주문 티켓 생성 → 관리자 완료 → 구매로그만 생성
10. 취소/자동 만료 시 잔액 환불 및 재고 복구
11. Basic / Basic Premium / Pro / Pro Premium 플랜별 명령/기능 제한 확인
12. Render 재시작 후 웹 DB와 봇 연결 상태 유지 확인

주의: 이 환경에서는 실제 Discord Gateway 로그인이나 실제 OAuth 토큰 교환을 실서버에서 검증할 수 없으므로, 마지막 단계는 본인 테스트 서버에서 진행하세요.
